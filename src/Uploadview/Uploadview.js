import React from "react";
import NavHead from "../PostView/NavHead";
import UploadForm from "./UploadForm";

export default function Uploadview() {
  return (
    <div>
      <NavHead />
      <UploadForm />
    </div>
  );
}
